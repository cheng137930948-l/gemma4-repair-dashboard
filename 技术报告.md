# 技术报告 · Gemma 4 产线维修 AI Agent 看板

> 参赛赛道：AI Agent ｜ 模型：Google **Gemma 4（`gemma-4-31b-it`）** ｜ 形态：可离线运行的静态前端 + 可选真实后端
> 在线演示：`https://cheng137930948-l.github.io/gemma4-repair-dashboard/` ｜ 代码仓库：本仓库

---

## 1. 一句话概述

把"产线维修课"每天靠人工 Excel 汇总的维修数据，做成一套 **Gemma 4 驱动的智能体看板**：实时计算修复率 / WIP / 超期 / 物料闭环，并由 Gemma 4 依据 `SKILL.md` 规则自动产出**结论前置、可执行**的管理汇报与异常预警，把"看数据"升级为"给决策"。

## 2. 背景与痛点

电子厂 PCBA 维修现场的真实痛点：

| 痛点 | 现状 | 业务后果 |
|---|---|---|
| 数据分散 | MES / EIP / 本地 Excel / 人工记录各一套 | 班组长每天手工拼表 1–2 小时 |
| 修复率不透明 | 进/出板靠事后统计 | 待修积压（WIP）发现滞后 |
| 超期无预警 | 未结天数靠人看 | 老化板卡拖到 P0 才暴露 |
| 汇报靠人写 | 主管汇报口径不一 | 结论主观、改善建议空泛 |

目标：用一个 **Agent + Skill** 的方案，让"取数 → 算指标 → 出结论 → 提预警"端到端自动化。

## 3. 系统架构

```
┌──────────────────────────── 前端（静态，可离线 / GitHub Pages）────────────────────────────┐
│  index.html（三看板入口）                                                                  │
│   ├─ 产线维修综合看板_v3.html ── buildAIPayload() ─┐                                        │
│   ├─ 修复率看板.html（数字孪生：WIP/修复率/不良）  │                                        │
│   └─ 产线维修课领退料看板.html                     │                                        │
│                                                    ▼                                        │
│                                          gemma_adapter.js                                   │
│                              （tool-registry + planner + trace 的智能体推理层）            │
│                                                    │                                        │
│                                gemma_config.js（demoMode 开关 / 模型配置）                  │
└────────────────────────────────────────────────────┼───────────────────────────────────────┘
                                                       │ demoMode=false
                                                       ▼
┌──────────────────────────── 可选真实后端（backend/, Flask）────────────────────────────────┐
│  report_server.py ──→ google-genai SDK ──→ Gemma 4（gemma-4-31b-it, generateContent）       │
│        ▲                                                                                     │
│  skills/repair-report/SKILL.md  ──（整段注入为 Prompt）──┘                                   │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

两条链路同源：无论 Demo 模式还是真实后端，分析规则都来自同一份 `SKILL.md`，保证"离线可演示、在线可复现"。

## 4. AI Agent 设计（核心）

`gemma_adapter.js` 在前端实现了一个**可观测的智能体推理层**，即使离线也能展示 Agent 行为：

1. **工具注册表（tool-registry）**：`get_wip`、`get_defect_detail`、`get_material_overdue` 等工具，把看板原始数据封装成可被"调用"的能力。
2. **规划器（planner）**：根据报告类型 / 用户提问选择并编排工具调用顺序。
3. **推理轨迹（trace）**：每一步"选择了哪个工具、得到了什么中间结论"都被记录，可在演示时展开，证明这是**多步推理**而非一次性套模板。
4. **结构化产出**：按 `SKILL.md` 的 9 段式结构（摘要 / 口径 / 指标 / 预警 / 风险 / 原因 / 建议 / 跟进 / 数据缺口）输出。

> 示例：综合看板的 `buildAIPayload()` 先用 `computeWipBoard()` 把工单状态聚合成 WIP 板（总量 / 机种分布 / 老化分桶 / 超期清单 / 积压趋势 / 修复率），再交由 planner 调 `get_wip` 生成结论："近期闭环工单修复率约 **81%**（已修 13 / 报废 3），关注报废根因"——与修复率看板的整体修复率口径（42/52≈81%）一致。

## 5. 关键工程取舍：从 ADK 到 Gemma 4

生产环境原系统采用 **Google ADK（Agent Development Kit）** 的 Agent + function calling 模式：

```python
from google.adk import Agent
from google.adk.tools import skill_toolset
root_agent = Agent(model="gemini-2.5-pro",
                   tools=[skill_toolset.SkillToolset(skills=[repair_skill])])  # 依赖 function calling
```

但 **Gemma 4 是开放权重模型，在 Gemini API 上仅支持 `generateContent` 纯文本生成，不支持 `tools=`（function calling）与 `system_instruction=`**，无法直接把 ADK 的 `model` 换成 `gemma-4-31b-it`。

本参赛后端做了 **Gemma 合规的等价实现**——**用 Prompt 注入替代工具调用**：

```python
from google import genai
client = genai.Client(api_key=GOOGLE_API_KEY)
prompt = SKILL_MD + "\n\n【看板数据】\n" + board_json   # 把 Skill 规则整段拼进 prompt
resp = client.models.generate_content(model="gemma-4-31b-it", contents=prompt)
```

没有用到任何 Gemini 独占特性，因此把 `MODEL_NAME` 设为 `gemma-4-31b-it` 即可直接运行。功能上等价于"Skill 驱动的维修分析"，同时完全符合 Gemma 4 的接口约束。这份取舍即是本项目的技术深度所在。

## 6. Skill 机制（`repair-report/SKILL.md`）

以 **Markdown Skill** 定义分析规则，作为 Prompt 文本注入，模型据此生成结构化汇报，关键约束包括：

- **禁止编造数字**：用户未提供的数据不得编造，无法确认须标注"数据未提供"。
- **字段识别与别名映射**：自动归一化工单号 / 未结天数 / 进出板时间 / 维修员等字段的多种叫法。
- **分级预警规则**：未结 >7/15/30 天 → P2/P1/P0；同一不良 ≥3/5 片 → P2/P1；当天未闭环影响维修 → P1。
- **改善建议必须可执行**：要有责任方向、优先级、建议完成时间与验证方式，杜绝"加强管理"式空话。

## 7. 数据与安全（脱敏方案）

面向公开参赛仓库做了系统性脱敏：

- 全量改用程序生成的**脱敏演示数据**（虚构机种 KSK/ABC/XYZ/DEF/GHJ、虚构维修员、虚构日期），三看板开箱即用。
- 移除内部代号、真实姓名、内网绑定服务（`localhost:8787` 默认关闭，靠 `RDC_ENABLE_BINDING_SERVER` 开关门控）、第三方接口默认值。
- 真实 API Key 仅存本地 `backend/.env`（`.gitignore` 排除），仓库仅留 `.env.example` 占位。
- Webhook 推送在 Demo 模式下只生成预览、不真实发送。

## 8. 量化价值

| 指标 | 改善方向 |
|---|---|
| 班组长每日手工拼表 | 1–2 小时 → 自动生成，≈ 0 |
| 待修积压（WIP）发现 | 事后统计 → 实时看板 + 趋势预警 |
| 超期老化板卡 | 人工巡检 → P0/P1/P2 自动分级预警 |
| 管理汇报 | 口径不一、结论主观 → Skill 规则统一、结论前置、建议可执行 |
| 部署成本 | 静态前端，零后端即可离线演示，可一键上 GitHub Pages |

## 9. 如何复现

**离线演示（零配置）**：`python -m http.server 8123` → 打开 `http://127.0.0.1:8123/index.html` → 进入三看板。

**真实 Gemma 4**：
```bash
cd backend && pip install -r requirements.txt
cp .env.example .env   # 填入 https://aistudio.google.com/apikey 申请的 GOOGLE_API_KEY
python report_server.py            # http://127.0.0.1:8000
# 将 gemma_config.js 的 demoMode 改为 false，前端即对接真实 Gemma 4
```

## 10. 局限与展望

- **当前局限**：受 Gemma 4 接口约束，真实链路为单轮 `generateContent`，agentic 的多步规划主要体现在前端适配层；Demo 模式为规则模拟。
- **下一步**：① 在后端用"Prompt 自反思 + 多轮调用"补足 agentic 闭环；② 接入真实 MES/EIP 数据源（保留脱敏开关）；③ 把推送从预览升级为可审计的真实闭环（带审批）。

---

*本报告与代码仓库、5 分钟演示视频、在线演示链接共同构成参赛交付物。所有数据均为脱敏演示数据，不可用于生产环境。*
